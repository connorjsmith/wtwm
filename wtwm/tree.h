#pragma once
// each desktop is its own tree
// should deal with manipulating the tree (insertions, deletions) and resizing nodes/splits

/* Needed functions:
 *  - Remove/Add window from tree, preselect insertion points
 *  - Setting split direction
 *  - resizing splits
 */

class WTree {

};
