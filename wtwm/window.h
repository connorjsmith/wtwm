#pragma once

// each window is a node in a desktop tree
// should deal with receiving focus etc. on a node-by-node basis
// also showing preselect feedback, window/rectangle helper functions etc.
// also deals with windows not associated with a tree (ie floating windows)

class WNode {

};