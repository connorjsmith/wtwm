super + {h,j,k,l}
    bspc node -f {west,south,north,east}

super + {0,1,2,3}
    bspc node -f {west,south,north,east}

ctrl + alt + {1,2}
    test command {a, 1}

alt + ctrl + {1, 2}
    test command {a, 2}